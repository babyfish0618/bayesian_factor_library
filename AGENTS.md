本文件定义 AI / Codex 在本仓库中的协作规范。

所有 AI 在开始任何任务前必须阅读本文件，并遵守其中规则。

AI 必须在开始任何任务前先阅读：

- AI_CONTEXT.md
- AGENTS.md
- AI_LOG.md

本文件不包含具体业务逻辑，业务目标请参考：

- AI_CONTEXT.md
- README.md
- docs/ 目录中的设计文档



---

# 一、AI 工作原则

AI 在本仓库中的目标是：

1. 帮助理解代码
2. 改进代码可读性
3. 实现新功能
4. 修复 bug
5. 编写必要文档

AI 的所有行为必须遵循：

- **最小修改原则**
- **代码可读性优先**
- **不破坏现有逻辑**
- **所有修改可追踪**

---

# 二、修改代码前的步骤

AI 在修改代码前必须执行：

1. 阅读 `AI_CONTEXT.md`
2. 阅读 `README.md`
3. 阅读 `AI_LOG.md`
4. 浏览 `src/` 目录核心代码
5. 理解配置文件（如 config.yaml）

如果对项目结构不清楚，应先向用户确认。

补充规则：

- `archive/`、`__archive/`、`docs/_archive/` 属于历史遗留代码/文档，默认可跳过，不作为当前主链路实现依据。
- 仅当用户明确要求排查历史实现时，才进入上述目录读取或修改。

---

# 三、代码修改原则

## 1 最小修改原则

除非用户明确要求：

- 不进行大规模重构
- 不改变代码结构
- 不删除用户代码

## 2 不改变业务逻辑

默认情况下：

- 不修改核心算法
- 不改变字段含义
- 不改变数据结构

## 3 修改必须可解释

AI 修改代码后必须说明：

- 修改了哪些文件
- 为什么修改
- 影响范围

---

# 四、代码风格

AI 编写代码必须保证：

- 清晰变量名
- 合理函数长度
- 清晰模块结构

避免：

- 过深嵌套
- 过长函数
- 难以理解的表达式

---

# 五、注释规范

AI 必须补充必要注释。

重点解释：

- 模块用途
- 类职责
- 函数输入输出
- 关键业务规则
- 非直观实现逻辑

### 函数 docstring 示例

```python
def process_data(data: pd.DataFrame) -> pd.DataFrame:
    """
    对输入数据进行清洗和转换。

    Parameters
    ----------
    data : DataFrame
        原始数据

    Returns
    -------
    DataFrame
        清洗后的数据
    """
```

避免：
逐行翻译代码
无意义注释

# 六、AI_LOG.md 记录规则

所有 AI 修改必须记录在 AI_LOG.md。
记录必须追加，不允许删除历史。

```
记录格式
## YYYY-MM-DD HH:MM

### 时间
YYYY-MM-DD HH:MM

### 本次任务
简要说明任务

### 修改文件
- file1.py
- file2.py

### 主要改动
- 修改内容
- 新增功能
- 重构说明

### 测试情况
是否新增测试：是 / 否
是否运行测试：是 / 否
测试结果：通过 / 未运行 / 失败

### 风险与待办
- 潜在问题
- 后续建议
```

不允许编造测试结果。

# 七、代码质量要求

AI 编写代码时必须：

- 优先保证可读性
- 结构清晰
- 易于维护

必要时可以：

- 拆分函数
- 优化变量命名
- 添加解释性注释

# 八、错误处理原则

AI 编写代码必须考虑：

- 输入异常
- 缺失字段
- 网络错误
- 数据格式问题

不要让程序因为单个错误直接崩溃。

# 九、日志与调试

代码应具备基本日志能力。
推荐：

- 使用 logging
- 输出关键步骤信息
- 输出错误信息

# 十、AI 行为约束

AI 不允许：

- 删除 AI_LOG.md
- 修改 AI_CONTEXT.md 的业务含义
- 编造不存在的数据来源
- 引入不必要的复杂依赖
- 进行大规模重构

# 十一、不确定时的处理方式

如果 AI 不确定：

- 需求含义
- 数据结构
- 字段含义
- 项目架构

应：

- 明确说明不确定点
- 提出可能方案
- 请求用户确认

# 十二、输出要求

AI 在完成任务后必须说明：

- 修改文件列表
- 修改目的
- 修改内容
- 潜在风险
- 下一步建议

不要只输出代码。

# 十三、优先级

AI 在执行任务时应遵循：

- 正确性
- 可读性
- 稳定性
- 性能
